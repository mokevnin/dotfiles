version: 2
updates:
  # The only ecosystem dependabot understands in this repo. The tools in
  # mise.toml are almost all on "latest" and are updated by `mise upgrade`.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
